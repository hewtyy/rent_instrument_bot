# 🔧 Tool Rent Bot - Телеграм-бот для учёта аренды инструментов

Простой и удобный бот для управления арендой инструментов с автоматическими уведомлениями, отчётами и каталогом.

## 🚀 Быстрый старт

### 1. Клонирование и настройка
```bash
# Клонируйте репозиторий
git clone https://github.com/hewtyy/rent_instrument_bot.git
cd bot_rent_instr

# Создайте файл с настройками
cp bot/.env.example bot/.env
```

### 2. Настройка бота
Отредактируйте файл `bot/.env`:
```env
BOT_TOKEN=ваш_токен_от_BotFather
TZ=Asia/Tokyo
# ADMIN_ID=123456789  # опционально, для админ-отчётов
# ADMIN_IDS=123456789,987654321  # ID администраторов через запятую (ОБЯЗАТЕЛЬНО!)
```

**⚠️ Важно:** Обязательно настройте `ADMIN_IDS` - без этого бот будет недоступен!

### 3. Запуск
```bash
docker compose up -d --build
```

### 4. Проверка работы
```bash
# Посмотреть логи
docker compose logs -f bot

# Проверить статус
docker compose ps
```

## 📋 Что умеет бот

### ✨ Основные функции
- **Добавление аренды** - просто отправьте `Инструмент 500` или `Инструмент` (если есть в каталоге)
- **Автоматические уведомления** - через 24 часа бот напомнит о продлении или возврате
- **Ежедневные отчёты** - в 21:00 присылает сводку по активным арендам и выручке
- **Каталог инструментов** - ведите базу цен на инструменты
- **Детальная информация** - залог, способ оплаты, доставка/самовывоз, адрес

### 🎯 Процесс создания аренды
1. Отправьте `Перфоратор 500` или `Перфоратор` (если есть в каталоге)
2. Укажите залог (введите сумму или "Без залога")
3. Выберите способ оплаты (наличные/перевод)
4. Выберите доставку или самовывоз
5. Если доставка - введите адрес
6. Готово! Аренда создана

## 🔐 Настройка доступа

### Добавление администраторов
1. **Получите ID пользователя** - отправьте `/start` боту @userinfobot
2. **Добавьте в `.env`**:
   ```env
   ADMIN_IDS=123456789,987654321,555666777
   ```
3. **Перезапустите бота**:
   ```bash
   docker compose restart bot
   ```

### Управление доступом
- **Только админы** могут использовать бота
- **Обычные пользователи** получат сообщение о запрете доступа
- **ID админов** отображаются в сообщении об ошибке

## 🎮 Управление

### Главное меню
- **📋 Список аренд** - просмотр активных аренд с оставшимся временем
- **📊 Отчёт сейчас** - отчёт за сегодняшний день
- **📅 Отчёт по дате** - отчёт за выбранную дату
- **📚 Каталог** - управление каталогом инструментов
- **⬆️ Импорт CSV** - импорт каталога из файла
- **💵 Установить цену** - добавление инструмента в каталог

### Команды
- `/start` - главное меню
- `/list` - список активных аренд
- `/catalog` - каталог инструментов
- `/setprice Название Цена` - добавить в каталог
- `/report_today` - отчёт за сегодня
- `/report YYYY-MM-DD` - отчёт за дату
- `/expire_last` - тест уведомления
- `/reset_db` - очистка базы данных

## 📊 Каталог инструментов

### Добавление инструментов
1. **Через команду**: `/setprice Перфоратор Bosch 500`
2. **Через CSV файл**: отправьте файл в чат боту
3. **Через импорт**: поместите `catalog.csv` в папку проекта

### Формат CSV файла
```csv
Название,Цена
Перфоратор Bosch,500
Шуруповёрт Makita,300
Лазерный уровень,400
```

### Управление каталогом
- Просмотр: `/catalog` или кнопка "📚 Каталог"
- Редактирование: выберите инструмент → изменить название/цену/удалить
- Импорт: отправьте CSV файл в чат

## ⏰ Уведомления и расписание

### Автоматические уведомления
- **Через 24 часа** после создания аренды бот пришлёт уведомление с кнопками:
  - ✅ **Продлить аренду** - добавляет ещё 24 часа
  - ❌ **Забрал инструмент** - завершает аренду

### Ежедневные отчёты
- **21:00** - отчёт пользователям с активными арендами
- **23:59** - фиксация выручки за день

### Настройка времени
Измените `TZ` в файле `bot/.env`:
```env
TZ=Europe/Moscow    # Москва
TZ=Asia/Tokyo       # Токио (по умолчанию)
TZ=America/New_York # Нью-Йорк
```

## 💾 Хранение данных

### База данных
- **Файл**: `rentals.db` (SQLite)
- **Расположение**: `/app/data/` (внутри контейнера)
- **Данные**: аренды, выручка, каталог инструментов

### Структура данных
- **Аренды**: инструмент, цена, залог, способ оплаты, доставка, адрес, время
- **Выручка**: дата, сумма, источник
- **Каталог**: название, цена

## 🛠 Установка и настройка

### Требования
- Docker и Docker Compose
- Токен бота от @BotFather

### Установка Docker

#### Windows
1. Скачайте Docker Desktop с официального сайта
2. Установите и запустите
3. Включите WSL2 (если нужно)

#### macOS
1. Скачайте Docker Desktop с официального сайта
2. Установите и запустите

#### Linux (Ubuntu)
```bash
# Установка Docker
sudo apt update
sudo apt install -y ca-certificates curl gnupg
sudo install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo $VERSION_CODENAME) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
sudo usermod -aG docker $USER

# Проверка
docker version && docker compose version
```

## 🔧 Управление ботом

### Основные команды
```bash
# Запуск
docker compose up -d --build

# Остановка
docker compose down

# Перезапуск
docker compose restart bot

# Просмотр логов
docker compose logs -f bot

# Статус контейнеров
docker compose ps
```

### Импорт каталога с компьютера
```bash
# Linux/macOS
docker compose cp ./catalog.csv bot:/app/data/catalog.csv

# Windows PowerShell
docker compose cp .\catalog.csv bot:/app/data/catalog.csv
```

### Обновление
```bash
# Получить обновления
git pull

# Пересобрать (если изменились зависимости)
docker compose up -d --build

# Или просто перезапустить
docker compose restart bot
```

## 🐛 Решение проблем

### Частые проблемы

**Бот не отвечает**
```bash
# Проверьте логи
docker compose logs bot

# Перезапустите
docker compose restart bot
```

**Неправильное время в отчётах**
- Проверьте `TZ` в `bot/.env`
- Перезапустите: `docker compose restart bot`

**Ошибка "Cannot connect to Docker daemon"**
- Запустите Docker Desktop
- На Linux: `sudo systemctl start docker`

**Ошибки HTML в сообщениях**
- Используйте только поддерживаемые теги: `<b>`, `<i>`, `<code>`, `<pre>`

### Полезные команды для отладки
```bash
# Войти в контейнер
docker compose exec bot bash

# Проверить базу данных
docker compose exec bot sqlite3 /app/data/rentals.db ".tables"

# Посмотреть содержимое .env
docker compose exec bot cat /app/.env
```

## 📁 Структура проекта
```
bot_rent_instr/
├── bot/
│   ├── main.py           # Основной файл бота
│   ├── bot_handlers.py   # Обработчики сообщений
│   ├── database.py       # Работа с базой данных
│   ├── scheduler.py      # Планировщик задач
│   ├── utils.py          # Вспомогательные функции
│   ├── requirements.txt  # Зависимости Python
│   └── .env             # Настройки (создаёте сами)
├── docker-compose.yml    # Конфигурация Docker
├── Dockerfile           # Образ для контейнера
└── README.md           # Этот файл
```

## 🤝 Поддержка

Если у вас возникли проблемы:
1. Проверьте логи: `docker compose logs bot`
2. Убедитесь, что Docker запущен
3. Проверьте настройки в `bot/.env`
4. Попробуйте перезапустить: `docker compose restart bot`

## 📝 Лицензия

Этот проект распространяется свободно для личного и коммерческого использования.

---

**Удачного использования! 🚀**